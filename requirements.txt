# Runtime dependencies for the FastAPI server (server.py / server_llama.py).
# All versions confirmed working with Python 3.13.5 (local venv).
# This is what Render installs — kept intentionally slim.
#
# For local data-ingestion work (build_supabase.py, build_chromadb.py,
# character_profiler.py) also install:
#   pip install -r requirements-dev.txt

# ── CPU-only PyTorch wheel ─────────────────────────────────────────────────────
# Directs pip to the PyTorch CPU index so sentence-transformers pulls a CPU
# torch build (~200 MB) instead of the default GPU build (~2 GB).
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.135.1
uvicorn==0.41.0
websockets==15.0.1

# ── LLM / Groq client ─────────────────────────────────────────────────────────
openai==2.26.0
jiter==0.13.0

# ── Supabase backend ──────────────────────────────────────────────────────────
supabase==2.28.0

# ── Embeddings ────────────────────────────────────────────────────────────────
# torch is a transitive dep of sentence-transformers — not listed directly.
sentence-transformers==5.2.3

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.2.2
numpy==2.4.2
pydantic==2.12.5
