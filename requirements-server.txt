# Server-only requirements — no torch / transformers / peft.
# Use this for deployment (Docker, Render, Railway, etc.).
# For local dev / fine-tuning use requirements.txt instead.

chromadb==1.5.2
fastapi==0.135.1
httpx==0.28.1
numpy==2.4.2
openai>=1.30.0
pandas>=2.0.0
python-dotenv==1.2.2
uvicorn[standard]==0.41.0
